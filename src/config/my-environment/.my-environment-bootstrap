#!/usr/bin/env sh
# shellcheck shell=sh disable=SC1091
#
# .my-environment-bootstrap — PONTO DE ENTRADA ÚNICO
#
# Path fixo em ~/.config/.my-environment-bootstrap.
# Se o diretório de instalação mudar, atualize APENAS este arquivo.
#
# Uso em scripts:
#   . "${HOME}/.config/.my-environment-bootstrap"

ENVIRONMENT_ROOT="${HOME}/.config/my-environment"
. "${ENVIRONMENT_ROOT}/sh/bootstrap.sh"
